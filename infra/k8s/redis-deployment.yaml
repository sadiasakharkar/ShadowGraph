apiVersion: apps/v1
kind: Deployment
metadata:
  name: shadowgraph-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shadowgraph-redis
  template:
    metadata:
      labels:
        app: shadowgraph-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: shadowgraph-redis
spec:
  selector:
    app: shadowgraph-redis
  ports:
    - port: 6379
      targetPort: 6379
